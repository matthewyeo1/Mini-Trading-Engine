// Public module header for velox::feed
#pragma once

// If concrete headers exist in include/velox/feed/, pull them in.
#if defined(__has_include)
#  if __has_include("velox/feed/feed_handler.hpp")
#    include "velox/feed/feed_handler.hpp"
#  endif
#endif

namespace velox {
namespace feed {
// Module-level forward declarations and utilities can go here.
}
}
